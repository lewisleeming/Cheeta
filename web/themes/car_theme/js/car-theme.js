/**
 * @file
 * car_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.carTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
